// gameState.js
export const canvas = document.getElementById("pong-canvas");
export const ctx = canvas.getContext("2d");

export function initializeGameState() {
  // Aucune initialisation nécessaire côté client pour l'instant
}